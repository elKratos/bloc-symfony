{% extends 'base-admin.html.twig' %}

{% block title %}{{ type_flower.name }}{% endblock %}

{% block body %}

    <h1>Edit TypeFlower</h1>

    {{ include('type_flower/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-info btn-block mt-2 mb-2" href="{{ path('type_flower_table') }}">back to list</a>

    {{ include('type_flower/_delete_form.html.twig') }}

{% endblock %}
